package bm.bookmark_manager.view.tag_list;

import android.content.Context;

import bm.bookmark_manager.common.model.Tag;

public class TagListWireframe {

    void presentEditInterface(Context context, Tag tag) {
//        Intent intent = new Intent(context, TagFormView.class);

//        intent.putExtra(TagFormPresenter.EXTRA_MODE, TagFormPresenter.MODE_EDIT);
//        intent.putExtra(TagFormPresenter.EXTRA_BOOKMARK, tag);

//        context.startActivity(intent);
    }

    public void presentAddInterface(Context context) {
//        Intent intent = new Intent(context, TagFormView.class);
//        intent.putExtra(TagFormPresenter.EXTRA_MODE, TagFormPresenter.MODE_NEW);
//        context.startActivity(intent);
    }
}
